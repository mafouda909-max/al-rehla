# Copy of .env.example for reference. The live `.env` holds the real values.
# Includes Neon Auth:
#   NEON_AUTH_BASE_URL       — your Neon Auth endpoint
#   NEON_AUTH_COOKIE_SECRET  — >=32 chars (openssl rand -base64 32)
